name: ToolKitX QA Workflow

on:
  pull_request:
    branches:
      - dev
  workflow_dispatch:

jobs:
  checks:
    uses: ./.github/workflows/checks.yaml

  build-android:
    uses: ./.github/workflows/build-android.yaml
